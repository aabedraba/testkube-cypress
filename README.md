# Example of Cypress Project

This is an example of a Cypress project. 

You can run the tests with 

```
npx cypress run
```

Or if you want to spin up the Cypress browser use 

```
npx cypress open
```
